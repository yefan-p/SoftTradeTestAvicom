<UserControl x:Class="SoftTradeTestAvicom.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SoftTradeTestAvicom.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="ButtonStyle">
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </UserControl.Resources>
    <StackPanel  HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button Command="{Binding GoManagerList}" Style="{StaticResource ButtonStyle}" >Managers</Button>
        <Button Command="{Binding GoProductList}" Style="{StaticResource ButtonStyle}" >Products</Button>
        <Button Command="{Binding GoClientList}" Style="{StaticResource ButtonStyle}" >Clients</Button>
    </StackPanel>
</UserControl>
